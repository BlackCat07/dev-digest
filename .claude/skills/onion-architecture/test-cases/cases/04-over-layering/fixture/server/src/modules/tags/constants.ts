/** Longest a tag name may be, matching the column width. */
export const TAG_NAME_MAX = 40;

/** Characters a tag name may carry before it is slugified. */
export const TAG_SLUG_PATTERN = /^[\p{L}\p{N} -]+$/u;
