export { TourSection, TourSection as default } from "./TourSection";
export type { TourSectionProps } from "./TourSection";
/* The heading id the on-this-page rail links to. Exported so the screen and this card
   read one function rather than two copies of one string — see `helpers.ts`. */
export { sectionHeadingId } from "./helpers";
