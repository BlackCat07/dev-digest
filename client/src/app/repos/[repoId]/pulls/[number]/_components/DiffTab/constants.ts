/**
 * Skeleton bars shown while the grouping loads.
 *
 * Three, matching the number of groups the response almost always has — so the
 * loading state occupies roughly the height the content will, and the tab does not
 * jump when it arrives.
 */
export const SKELETON_ROWS = 3;
