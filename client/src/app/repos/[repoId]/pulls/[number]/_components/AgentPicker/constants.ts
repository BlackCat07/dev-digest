/** Constants private to AgentPicker. */

/**
 * Panel width (px). Wider than the dropdown it replaces (250) because every row
 * now carries three things — a checkbox, the agent's name and that agent's mean
 * duration — where the old menu carried a label and a hint.
 */
export const PANEL_WIDTH = 300;

/** The agents screen, linked below the list (AC-51). */
export const AGENTS_ROUTE = "/agents";
