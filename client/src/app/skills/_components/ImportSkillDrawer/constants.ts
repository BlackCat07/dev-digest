/** Constants for the skill import drawer. */

/** File types the picker offers. A skill body is markdown; nothing else runs. */
export const ACCEPTED_EXTENSIONS = ".md,.mdx,.markdown,text/markdown";
